# 2026-09-05 修改日志

## 修复社区自研插件未安装

- 现象：`web` profile 未安装社区自研的两个插件（`dsh-plugin-manager`、`@ruihuahe/dsh-plugin-marketplace`），且 `community/plugins/tarballs/` 目录缺失，导致 seed 流程无法提供本地 file 依赖。
- 概述：构建打包社区自研插件 tarball，seed 重建 `web` profile，使自研插件通过本地 tarball 就位；同步保留 dsh-doc 的本地 python 引擎配置。
- 验证：`pnpm dsh web` 启动成功，三个自研插件（含 `dsh-codex-shell`）与 `dsh-doc` 均加载无报错。

## 优化 dsh-codex-shell 界面

- 现象：左侧栏为自绘深色 Codex 风格、与宿主界面不一致；右侧面板是悬浮浮层，不构成三栏工作区。
- 概述：左侧栏改为宿主原生皮肤（宿主主题令牌、原生区头/搜索/折叠轨道交互），保留插件特有功能并新增自带的工作区目录选择器；右侧面板停靠进宿主第三列，与列宽、拖拽、开合动画联动，加载后即为三栏工作区。
- 验证：插件单测 5 项全部通过；类型检查与打包校验通过；新包已同步到 `web` profile 并热更新生效。

## dsh-codex-shell 左侧栏改 Codex 式极简排布

- 现象：左侧栏工作区面板视觉杂乱（双行会话、彩色状态点、分组图标与计数、内嵌选择卡片）。
- 概述：工作区面板改为 Codex 左侧栏排布——常驻圆角搜索框、细字工作区标题、单行会话、悬停才显露时间与操作；添加工作区入口移至侧栏页脚并改为居中弹窗。
- 验证：插件单测 5 项全部通过；类型检查、打包校验通过；新包已同步到 `web` profile 并热更新生效。

## 启用 dsh-tool-session-query 跨会话搜索

- 概述：在 `web` profile 补丁层挂载 `@deepseek-ai/dsh-tool-session-query`（注册 session_search 等 5 个跨会话只读工具），并在后续补丁层把 `session-query-sqlite` 的 `openAt` 改为 `first-search` 以启用全文检索；同时把该工具包加入 base bundle 依赖清单并更新锁文件。
- 验证：热重载生效，`session_search` 实测返回既往会话命中；`Tool.listTools` 已列出 5 个会话工具。

## dsh-codex-shell 侧栏对齐 Codex 行高与排序细节

- 现象：会话行高不固定、组内会话顺序无规则、工作区名称为收起箭头而非文件夹图标、字体层级与截断不明确。
- 概述：会话/子代理行改为固定行高；会话先按工作区收纳、组内按最近使用排序；工作区名称前改为文件夹图标（展开=打开、收起=闭合）；明确工作区/会话/子代理三档字体层级，名称超长统一省略号截断。
- 验证：插件单测 5 项全部通过；类型检查、打包校验通过；新包已同步到 `web` profile 并热更新生效。

## 会话输入框补回形针图片上传按钮

- 现象：输入框仅支持拖拽/粘贴传图，缺少点击上传入口；模型不支持图片时仅提交后报错。
- 概述：在宿主 composer 工具栏补回形针点击上传按钮（隐藏 file input，与拖拽/粘贴共用同一 intake 路径，仅接受 png/jpeg/webp/gif）；模型不支持图片时沿用提交拒绝的本地化提示（当前模型不支持图片，请切换支持图片的模型）。
- 验证：ui-conversation 全部 355 项单测通过（新增 2 项）；类型检查通过；客户端 bundle 已重建，热更新生效。注意：入库前需补 Agent Note 与 web 快照校验。

## 回形针按钮的 Agent Note 与 web 快照

- 概述：更新既有 owner Agent Note（2026-07-22-web-multimodal-image-input-and-durable-attachments，中英双语，把「文件选择」从待办移入已交付）；安装本机缺失的 Playwright Chromium 后运行 web 快照校验，用 DSH_SNAPSHOT=refresh 重录含新按钮的 golden。
- 验证：Agent Note 格式门禁通过（686 篇全合规）；与回形针相关的 web 场景（question-composer、live-interactions、steering、queue-actions、lifecycle-chrome、feedback、plan-review 等）重录后通过。其余失败（如 shipped-composition 的 unknown tool bash、ptc-round、plugin-config）系仓库既有未提交改动与 Windows 环境所致，与本变更无关，已留待后续梳理。
