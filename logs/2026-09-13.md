# 2026-09-13 修改日志

## dsh-codex-left 代码治理：按职责重组目录并拆分超长文件

- 背景：昨日拆分出的 `dsh-codex-left` 沿用了旧包的文件布局——`src/client/` 根目录 13 个文件，且 `WorkspaceBrowser.tsx`(468 行)、`browser-tree.tsx`(316 行)、`sidebar-menus.spec.tsx`(270 行)、`ProjectPicker.tsx`(209 行) 超过「单文件 200 行」的硬性指标，文件夹也超过 8 个文件。
- 处置：`src/client/` 重组为按职责分层的目录——`browser/`（浏览器根组件、树体、渲染件、动作工厂、副作用、搜索外壳）、`state/`（持久化仓与纯投影）、`overlays/`（菜单浮层与弹窗，含 `dialogs/` 子目录）；超长文件按「契约 / 动作 / 副作用 / 渲染件」拆开，长测试按 describe 分块拆成两个规格文件；行为保持不变。
- 验证：`tsc -p tsconfig.client.json` 无错误；vitest 由 7 个文件 37 项增至 9 个文件 41 项全绿（新增根组件装配规格 4 项：项目分组渲染、会话打开、项目菜单、分组收起持久化）；`build` 与 `pack:check` 通过并重建 tarball；复查全部 TypeScript 文件 ≤200 行、每个目录 ≤8 个文件、无空目录残留。
- 影响范围：仅 `community/plugins/packages/codex-left/`（源码布局、测试、README 中英的「代码结构」小节与配对哈希）与 `community/plugins/tarballs/` 的对应产物；未改动 host 半边行为、Remote 契约与其它插件。
- 备注：重构期间发现上一轮迁移脚本用 PowerShell 处理「单元素嵌套数组」时会退化成逐字符替换，导致 3 个文件（prefs/groups/use-session-search）的单引号被替换成句点；已从 git 恢复并从源头重做路径改写，后续批量文本改写改用 Node 脚本执行。
