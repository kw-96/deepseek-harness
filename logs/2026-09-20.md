# 2026-09-20 开发日志

## 升级到官方 v0.1.6-alpha.2 底座（用户要求）

- 背景：官方发布 `dsh-v0.1.6-alpha.2`（887 个提交），本 fork 停在 `v0.1.6-alpha.1`，其上另有 213 个自研提交。
- 评估：官方增量 2622 个文件（+101460 / −23708 行），与本地改动重叠 106 个文件；合并基线解析为 alpha.1，三路合并可行；会话格式版本未变，历史会话与快照可读。
- 处置：在独立 worktree（`deepseek-harness-upgrade-alpha2`）以 `dev-upgrade-v0.1.6-alpha.2` 分支合并，解决 37 个冲突，一律采纳官方实现；ui-layout 回归官方右栏模型（本地 details 列与 bottom 轨道退役），随后把桌面壳标题栏重新接到官方 overlay 槽。
- 适配：会话列表的当前会话标记改按主视图持有关系派生；会话打开改走工作区导航面；插件设置槽随官方改名；生成物与 tsconfig 别名同步；四个 profile 分发 tarball 重打为 alpha.2，模板依赖与分发目录同步替换。
- 顺带修复：codex 导入卡片从官方 bundle 移到 profile 层——此前它会随 bundle 进入官方测试夹具，并因官方把该槽从 keyed 改为 list 而总是渲染，触发未 mock 的 Remote 请求，导致客户端测试成片失败。
- 验证：类型检查与全量构建通过；受影响包单测 176 + 61 + 151 项通过；生成物四项校验、tsconfig 别名校验、profile 组合自检（17 bundle / 173 插入行）全部通过。
- 遗留：生成文档 `docs/config-catalog.zh.md`、`docs/capability-seams.zh.md` 的中文版尚未跟进本地新增包；全量单测存在既有环境失败（Windows 沙箱 `CreateProcessAsUserW` 与 PTC 运行时，升级前基线同样失败，已同环境对照确认）。
- 影响范围：`packages/`、`apps/`、`community/profiles/web/`、`community/plugins/tarballs/`；运行时需重启 `dsh web` 才会加载新底座。

## 标题栏精简、插件管理收敛到官方、文档双语同步（用户要求）

- 处置：桌面标题栏去掉「底部栏」按钮（bottom 轨道已随 alpha.2 退役）；View 菜单的底部工作台/终端入口保留，仍作用于外部工作台注册的锚点。profile 下线自研 `dsh-plugin-manager`（插件管理改用官方原生页），并按用户要求下线第三方 `dsh-better-sidebar`（非官方维护，不再回归）——两者写入下线清单、清理补丁层中由 manager 写入的注解、删除其分发 tarball。两份生成文档的中文版补齐本地新增包内容。
- 验证：类型检查通过；ui-layout 单测 151 项通过；临时 DSH_HOME 跑 seed 得 16 个 bundle（两个下线插件均未出现）；全库 1062 对文档双语校验一致。
- 待办：桌面壳可执行文件已重建，待用户关闭在运行的壳后部署到主工作区。
- 影响范围：`packages/client/ui-layout`、`community/profiles/web/`、`community/plugins/tarballs/`、`docs/`。

## 清理四项文档门禁 + 部署新桌面壳（用户要求）

- 处置：清掉 `test:docs` 长期为红的四项门禁。type-equiv 的 `Workspace` 块与叙述按当前源码重记；链接检查对已删除的 GitHub 工作流路径加窄例外；历史日志中的裸提交标识改为等价文字；禁用词方面排除内置 Node 分发物与原样引入的第三方 skill，并把本仓自有包改为更名。门禁例外的决策与替代方案另附 Agent Note。
- 部署：按用户指定方式更新桌面壳可执行文件——先把在用的旧 exe 重命名为 `DeepSeek Harness.exe.pre-alpha2`（运行中的实例不受影响），再拉取仓库中的新 exe 就位。
- 验证：`test:docs` 20 项全绿（改动前 5 项失败）；类型检查通过；pre-commit 钩子全通过；新 exe 校验值与构建产物一致。
- 待办：用户关闭当前桌面壳实例并用新 exe 重启后，可删除 `DeepSeek Harness.exe.pre-alpha2`。
- 影响范围：`scripts/`、`docs/subsystems/workspace*.md`、`logs/`、`packages/session/session-import-codex`、`.agents/notes/`；主工作区 exe。

## 工单插件入口改挂官方右栏 + 自研插件接入官方安装入口（用户要求）

- 需求：把工单插件的入口注册到右侧栏，并去掉原先注入左侧栏页脚的那个入口。
- 处置：为工单插件补上客户端面与构建链，向官方右栏注册「工单」页面标签并内嵌既有控制面；同时移除注入左侧栏页脚的导航脚本与其装配点。另为工单插件与模式路由插件声明 bundle 元数据，使桌面端「添加插件」这类官方入口能接受它们——此前自研插件不满足入口的 bundle 判定，会被直接拒绝，表现为模式路由预设加载失败。
- 验证：两端类型检查通过；插件单测 20 文件 81 项全绿；从桌面端 profile 的安装路径实测两个插件均可加载；分发 tarball 同步重打。
- 待办：桌面端需重启才会重新扫描客户端插件表，右栏标签在重启后出现。
- 影响范围：`community/plugins/packages/workorder-agent`、`community/plugins/packages/mode-router`、`community/plugins/tarballs/`。

## 统一补齐三个自研插件的分发产物（用户要求）

- 背景：换主机部署桌面端时，插件只能从仓库里的分发产物安装（源码链接方式换机即失效）。核查发现桌面面板的分发件落后于源码，模式路由插件则从未打过分发件。
- 处置：三个插件统一重建并重打分发包——桌面面板补进 native worker 的源码与安装脚本（此前分发件缺这部分，装完无法部署 worker），并让分发件与已跟踪的客户端产物一致；模式路由新增分发包；工单插件重建后字节完全一致，确认已是最新。
- 验证：逐个校验分发包含 bundle 元数据、声明的补丁文件与运行入口；工单插件确认不含已删除的导航模块。
- 说明：桌面面板的 Node 面只有构建产物、没有对应源码，其 `lib` 必须留在版本控制内；工单插件结构不同（源码完整、产物不入库），两者不能按同一方式处理。
- 影响范围：`community/plugins/packages/desktop-panel`、`community/plugins/tarballs/`。

## 四个官方包恢复 npm 版本（用户要求）

- 背景：`community/plugins/tarballs/` 下四个 `@deepseek-ai/*` tarball 是仓库内构建的定制产物，当初为 Tauri 桌面壳等改动所打；该壳已退役，定制失去依据。
- 处置：下线这四个分发包，`community/profiles/web/` 模板改为引用 npm 官方 `0.1.6-alpha.2`。
- 验证：模板 JSON 合法；pre-push 全量构建与类型检查通过；PR 自检 head/base 后合入 dev。
- 待办：已 seed 过的机器上，profile 残留的 `file:` 指向已失效，需手工改成版本号。
- 影响范围：`community/plugins/tarballs/`、`community/profiles/web/`。

## 清理误提交产物、移动端适配迁往样式片段、下线 Codex 导入（用户要求）

- 处置：分三步收敛 fork 与官方底座的差异。其一，清掉一批不可达文件——无引用的包内分发产物、未接线的连接层夹具、误提交进源码目录的编译残留；顺带修正若干文档与测试中的字符损坏和写反的断言。其二，官方包内被改写的移动端适配样式全部还原为官方版本，改由插件样式片段在运行时下发，源码另存到 `community/styles/` 备查。其三，Codex 会话导入功能整体下线，源码、文档、生成入口与 profile 引用一并清除。
- 说明：Codex 导入原本要迁成自研插件，但其运行依赖 Typert 类型图，而社区插件目录是独立的 pnpm workspace，两边的类型图无法共用，迁移在结构上不成立，故改为直接移除。
- 待办：用户在手机端验收移动端适配表现；样式片段以类名模糊匹配方式覆盖，需留意选择器过宽带来的副作用。
- 影响范围：`packages/`、`community/styles/`、`community/profiles/web/`、`docs/`、`scripts/`。

## 重建 web 与桌面并部署新桌面壳（用户要求）

- 处置：重跑全量构建与桌面打包，把新产物覆盖到在用的桌面壳目录，旧文件保留为带时间戳的备份；壳外的未压缩资源逐项比对后确认一致，无需同步。
- 验证：构建与打包均成功退出；对照扫描确认新包已不含 Codex 导入的包名与服务标识，旧包在同一扫描下命中数百处，扫描方法本身以必然命中的对照串自证有效。
- 待办：桌面端重启后，设置页的 Codex 导入入口应消失。
- 影响范围：`apps/desktop/.desktop-build/official-desktop-alpha.2/app/resources/`。
