# The dsh-github bundle patch: mount the `ctx.github` capability seam.
# One insert; row order carries no load semantics (activation is
# service-availability driven), so installing the connector packages in any
# order composes the same host.

- insert:
    - id: github
      name: dsh-github
