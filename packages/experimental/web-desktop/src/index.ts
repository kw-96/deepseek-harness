/**
 * `@deepseek-ai/dsh-experimental-web-desktop` — Tauri native shell for `dsh web`.
 * Runtime lives in `src-tauri/`; this module is the workspace package face so
 * Host tsc and tsdown can include the package. It exports no runtime API.
 * @module @deepseek-ai/dsh-experimental-web-desktop
 */

export {}
