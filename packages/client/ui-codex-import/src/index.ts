/**
 * Codex import settings card plugin, node half. The empty apply keeps the
 * browser feature addressable from the host-owned Loader overlay.
 */

/** Host plugin body — Codex import card behavior exists only in the browser entry. */
export function apply(): void {}
